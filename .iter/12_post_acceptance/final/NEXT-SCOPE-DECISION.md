# 下一轮范围决策

## 结论
本轮 Maven 拆分已完成并通过编译/测试。下一轮继续做工程边界加固，不直接扩大业务功能范围。

## 下一轮 P1
- 给 `nexa-common` 补公共组件边界说明。
- 增加 Maven Enforcer 或等价校验，防止公共模块反向依赖业务包。

## 暂缓
- 进一步合并各子域 ExceptionHandler。原因：局部 handler 可能包含子域特定错误码/状态码，应该逐域审查后收敛。
