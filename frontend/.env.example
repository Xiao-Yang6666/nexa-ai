# Nexa 前端环境变量模板 — 复制为 .env.local 并填入真实值（.env.local 不进 git）
# 注意：NEXT_PUBLIC_* 是 next build 时烘进 bundle 的，改完必须重新 npm run build。

# 后端 API 地址。必须是浏览器所在设备能路由到的地址：
#   - 本地开发： http://localhost:8080
#   - 局域网联调： http://<服务器局域网IP>:8080（如 http://192.168.1.92:8080）
#   - 生产： https://api.your-domain.com
# 切忌用 docker 内部 IP（172.17.x），用户设备路由不到会 Failed to fetch。
NEXT_PUBLIC_API_BASE=http://localhost:8080

# 是否走 mock 桩数据：0=真后端，1=mock
NEXT_PUBLIC_USE_MOCK=0
